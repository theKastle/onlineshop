import { CartLine } from './cartLine';
export class Cart {

    // constructor() {
    //     this.cartLines = []
    // }

    // cartLines: CartLine[];
    // totalPrice(): number {
    //     var sum = 0;
    //     return this.cartLines.map(e => e.totalPrice()).reduce((sum, e) => {
    //         return sum + e
    //     }, 0)
    // }
}